// Socratic Chat system prompt. Enforces the Anti-Spoiler Rule at every tier.
// TODO: Phase 5 — implement full prompt with tier directives

export function buildCoachingPrompt(_hintTier: number, _problem: unknown): string {
  // TODO: Phase 5 — implement
  return ''
}
