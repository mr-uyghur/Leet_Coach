// Solution system prompt. Only called after solutionUnlocked === true.
// This is the ONLY prompt that bypasses the Anti-Spoiler Rule — gated by UI confirmation.
// TODO: Phase 5 — implement

export function buildSolutionPrompt(_problem: unknown): string {
  // TODO: Phase 5 — implement
  return ''
}
